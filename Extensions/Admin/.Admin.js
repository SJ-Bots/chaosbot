module.exports = {
  name: '\n**管理功能**',
  description: '這只是拿來分類用的\n它毫無作用',
  args: false,
  // usage: '',
  guildOnly: false,
  // cooldown: 0,
  // aliases: [''],
  // permissions: '',
  needSQL: false,
  authorOnly: false,
  execute(msg, args, prefix, command) {
    return;
  },
};
